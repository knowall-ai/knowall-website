# Format Check

Check code formatting with Prettier.

## Instructions

1. Run `npm run format:check`
2. If there are formatting issues, list the files that need formatting
3. Ask the user if they want to auto-fix by running `npm run format`
